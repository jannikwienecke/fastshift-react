import { DataProvider } from '@apps-next/core';
import {
  ViewDataProvider,
  ViewProvider,
} from 'libs/react/src/lib/view-provider';
import { getTasks } from './actions';

export const createGlobalConfig = (schema: any) => {
  return {
    // globalConfig
    // schema and stuff
  };
};
